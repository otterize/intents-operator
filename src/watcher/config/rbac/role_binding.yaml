apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: otterize-pod-watcher-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: otterize-pod-watcher-role
subjects:
  - kind: ServiceAccount
    name: otterize-pod-watcher
    namespace: otterize
